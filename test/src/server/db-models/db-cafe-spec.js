const request = require('supertest');
const express = require('express');
const assert = require('chai').assert;
const expect = require('chai').expect;
// const userModel = require(__dirname + '/../../../../src/server/db-models/db-user.js');
// const cafeModel = require(__dirname + '/../../../../src/server/db-models/db-cafe.js');

describe('Module', function() {
  describe('Cafe', function () {
    it('should define a new table', function () {
      
    });
    it('should have a User foreign key', function () {
      
    });
    it('should be exported', function () {
      
    });
  });
});