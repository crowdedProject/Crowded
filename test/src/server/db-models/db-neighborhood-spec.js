const request = require('supertest');
const express = require('express');
const assert = require('chai').assert;
const expect = require('chai').expect;
const neighborhoodModel = require(__dirname + '/../../../../src/server/db-models/db-neighborhood.js');

describe('Module', function() {
  describe('Neighborhood', function () {
    it('should define a new neighborhood table', function () {
      
    });
    it('should have ours and yelp neighorhood name columns', function () {
      
    });
  });
});